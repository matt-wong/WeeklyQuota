import { QuotaPlanPercentPipe } from './quota-plan-percent.pipe';

describe('QuotaPlanPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new QuotaPlanPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
