import { UnfinishedPlanPipe } from './unfinished-plan.pipe';

describe('UnfinishedPlanPipe', () => {
  it('create an instance', () => {
    const pipe = new UnfinishedPlanPipe();
    expect(pipe).toBeTruthy();
  });
});
