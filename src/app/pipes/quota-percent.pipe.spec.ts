import { QuotaPercentPipe } from './quota-percent.pipe';

describe('QuotaPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new QuotaPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
