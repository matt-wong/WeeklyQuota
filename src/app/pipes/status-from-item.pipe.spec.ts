import { StatusFromItemPipe } from './status-from-item.pipe';

describe('StatusFromItemPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusFromItemPipe();
    expect(pipe).toBeTruthy();
  });
});
